export enum TaskProgress {
  IDLE,
  PENDING,
  SUCCESS,
  ERROR,
}

export enum QuestionType {
  ANY = 'any',
  MULTIPLE = 'multiple',
  BOOLEAN = 'boolean',
}

export enum QuestionDifficulty {
  ANY = 'any',
  EASY = 'easy',
  MEDIUM = 'medium',
  HARD = 'hard',
}
